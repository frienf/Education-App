// Placeholder for markdown parsing utilities (if needed for custom rendering)
import type { Components } from "react-markdown";

// Basic configuration for ReactMarkdown (extend as needed)
export const markdownComponents: Partial<Components> = {
  // Add custom rendering for Markdown elements if required
};